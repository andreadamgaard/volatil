export type VinVisningType = {
  sku: string;
  handle: string;
  title: string;
  producent: string;
  type: string;
  tags: string[];
  published: boolean;
  price: number;
  image: string;
  navn: string;
};
